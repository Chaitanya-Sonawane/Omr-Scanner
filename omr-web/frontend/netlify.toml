[build]
  command = "npm run build && cp -r mobile dist/"
  publish = "dist"

[[redirects]]
  from = "/api/*"
  to = "https://omr-scanner-backend.onrender.com/api/:splat"
  status = 200
  force = true

[build.environment]
  NODE_VERSION = "18"
