services:
  - type: web
    name: omr-scanner-api
    runtime: python
    rootDir: .
    buildCommand: pip install -r requirements.api.txt
    startCommand: uvicorn api_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ALLOWED_ORIGINS
        value: https://omrsheetscanner.netlify.app
      - key: PYTHON_VERSION
        value: "3.11.9"
    autoDeploy: true
